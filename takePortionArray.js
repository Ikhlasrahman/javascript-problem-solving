//2. Take a portion of an array 
// (not the whole thing) without changing the original. Log the result.
const a = ['Yellow','White','Black']
const takePortion = a.slice(0,1);
console.log(takePortion)