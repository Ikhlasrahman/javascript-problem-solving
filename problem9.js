/** Using isWeekend and isHoliday,
 * print "No work today" if either one is true (use ||). */

const isWeekend = false;
const isHoliday = false;

if(isWeekend || isHoliday){
    console.log('No Work Today');
}else{
    console.log('Has Work');
}