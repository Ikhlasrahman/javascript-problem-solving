/**If the price is greater than 1000, 
 * print "Expensive", otherwise "Affordable" 
 * — write this using a ternary operator. */

const price = 5000;

console.log(price>1000?"Expensive":"Affortable");